---
title: Quickstart
description: "From zero to your agent answering health questions in under 3 minutes."
---

You'll need an iPhone with the **Wirebody** app installed, a Mac/Linux machine on the same Wi-Fi running an MCP-aware agent (Claude Code, Codex, Cursor, etc.), and Node 20+.

## 1 — Turn on the LAN server

<Steps>
  <Step title="Open Wirebody">
    Switch to the **Settings** tab.
  </Step>
  <Step title="Toggle Local LAN Server on">
    iOS will ask for **Local Network** permission the first time. Allow it.
  </Step>
  <Step title="Note the URL and token">
    The Settings screen now shows:

    - **URL** — something like `http://192.168.1.244:5606`
    - **Token** — a 22-character base62 string

    Tap either to copy it. A small `Copied` capsule confirms.
  </Step>
</Steps>

<Note>
The LAN server only runs while the Wirebody app is foregrounded on the iPhone. iOS suspends background apps, so the server stops the moment you switch apps or lock the phone. This is a platform constraint, not a bug.
</Note>

## 2 — Verify it works with `curl`

From any other device on the same Wi-Fi:

```bash
curl -H "Authorization: Bearer <token>" http://<ip>:<port>/
```

You should get back:

```json
{"name":"Wirebody","sampleEncoding":"columnar-v1","version":"1.0","workoutCount":124}
```

If you get `401 unauthorized`, the bearer token doesn't match. If the connection refuses, the iPhone is locked or the app is backgrounded — bring it back to the foreground.

## 3 — Install the MCP server

The MCP server is a tiny Node program that proxies the LAN endpoints to any MCP-aware agent.

<CodeGroup>
```json Claude Code (~/.claude/mcp_servers.json)
{
  "mcpServers": {
    "wirebody": {
      "command": "npx",
      "args": ["-y", "wirebody-mcp"],
      "env": {
        "WIREBODY_URL": "http://192.168.1.244:5606",
        "WIREBODY_TOKEN": "<paste-your-token>"
      }
    }
  }
}
```

```json Codex (~/.codex/config.json)
{
  "mcpServers": {
    "wirebody": {
      "command": "npx",
      "args": ["-y", "wirebody-mcp"],
      "env": {
        "WIREBODY_URL": "http://192.168.1.244:5606",
        "WIREBODY_TOKEN": "<paste-your-token>"
      }
    }
  }
}
```

```json Cursor (.cursor/mcp.json)
{
  "mcpServers": {
    "wirebody": {
      "command": "npx",
      "args": ["-y", "wirebody-mcp"],
      "env": {
        "WIREBODY_URL": "http://192.168.1.244:5606",
        "WIREBODY_TOKEN": "<paste-your-token>"
      }
    }
  }
}
```
</CodeGroup>

Restart the agent. The tools `status`, `list_workouts`, `get_workout`, `list_quantity_types`, `get_quantity_series`, `list_sleep_sessions`, and `get_day_snapshot` should appear in its tool list.

## 4 — Ask your first question

In Claude Code (or any agent), try:

> Summarize my last 3 workouts and tell me whether my pace at threshold heart rate has improved.

The agent calls `list_workouts(limit: 3)`, follows up with `get_workout(uuid)` for each, computes pace-per-km at avg HR ~165, and tells you. No spreadsheet, no copy-paste.

Or:

> Give me a single-day summary of yesterday. Just the highlights.

The agent calls `get_day_snapshot(date: "2026-05-10")` and replies with sleep / activity / heart / mobility / workouts.

## What's next

- [Wire format](/concepts/wire-format) — why the JSON looks the way it does
- [API reference](/api-reference/overview) — every endpoint, every parameter
- [MCP integration](/mcp-overview) — tools, error codes, environment variables
